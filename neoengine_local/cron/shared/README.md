# NeoEngine product-org shared cron controls

Shared prompt/control invariants for NeoEngine and NeoWealth founder watchdogs
and PR/CI traffic-controller runtime wrappers. The validator checks that the
shared control file includes the durable controller-inbox handoff and structured
branch namespace collision evidence requirements.
